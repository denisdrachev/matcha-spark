CREATE TABLE IF NOT EXISTS rating (
                                      id INT AUTO_INCREMENT PRIMARY KEY,
                                      rating INT,
                                      login INT
) ;